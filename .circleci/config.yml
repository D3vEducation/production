version: 2
jobs:
  build:
    branches:
      only:
        - master
    working_directory: ~/tmp
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm install
      - run: scp -o StrictHostKeyChecking=no -r ~/tmp/* $DROPLET_USER@$DROPLET_IP://root/production
